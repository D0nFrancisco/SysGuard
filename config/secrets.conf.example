# Copia este archivo a config/secrets.conf (mismo directorio) y completa
# los valores reales. config/secrets.conf está en .gitignore y nunca debe
# subirse al repositorio.

WEBHOOK_URL="https://example.com/webhook"
