# Umbrales de alerta (porcentaje)
CPU_THRESHOLD=80
RAM_THRESHOLD=85
DISK_THRESHOLD=90

# Directorio de logs y reportes
LOG_DIR="./logs"
REPORT_DIR="./reports"

# Email de alertas (puedes dejarlo así por ahora)
ALERT_EMAIL="tu@email.com"

# Intervalo de chequeo en modo continuo (segundos)
CHECK_INTERVAL=60
